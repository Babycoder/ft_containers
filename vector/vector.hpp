#ifndef VECTOR_HPP
#define VECTOR_HPP

#include <iostream>



namespace ft{
    
    //template < class T, class Alloc = allocator<T> >
    class vector{
        private:

        public:
        //vector (const allocator_type& alloc = allocator_type())
        vector();
        ~vector();
    };
}

#endif